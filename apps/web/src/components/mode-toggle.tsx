/**
 * Intentionally empty.
 *
 * TheSEOSaaS v0.1 is light mode only. The design file has no dark variants, so
 * there is nothing to toggle to — adding one means authoring a full .dark
 * token block in packages/ui/src/styles/globals.css first.
 *
 * Kept as a signpost so the scaffold's toggle isn't reintroduced by accident.
 */
export {};
